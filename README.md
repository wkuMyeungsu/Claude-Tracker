# ClaudeTray

Windows 시스템 트레이에서 Claude Code의 사용량(quota)과 추가 결제 크레딧을 실시간으로 추적하는 Qt6 기반 모니터링 프로그램입니다.

[![Download Windows](https://img.shields.io/badge/Download-Windows_v1.0.0-0078D6?style=for-the-badge&logo=windows&logoColor=white)](https://github.com/wkuMyeungsu/Claude-Tracker/releases/tag/v1.0.0)
![badge](https://img.shields.io/badge/Qt-6.x-green) ![badge](https://img.shields.io/badge/platform-Windows-blue) ![badge](https://img.shields.io/badge/language-C%2B%2B17-orange)

---

## 주요 기능

- **실시간 사용량 모니터링**: 5시간 및 7일 토큰 사용률을 실시간 게이지바로 시각화하고 초기화(리셋)까지 남은 시간을 카운트다운으로 보여줍니다.
- **추가 결제 크레딧 추적**: 기본 제공량을 모두 소모한 후 사용하는 추가 결제용 크레딧($)의 남은 한도와 사용액을 표시합니다.
- **오프라인 로컬 추적**: API 응답 지연이나 네트워크 오프라인 상태에서도 로컬에 저장되는 대화 로그 파일들을 분석하여 소모 비용(Delta)을 실시간으로 계산해 병합합니다.
- **최근 사용 모델 표시**: 대시보드 창 하단에 가장 최근에 대화를 나눈 모델 식별자(예: `sonnet-5`)를 감지하여 표시하며, 마우스 호버 시 풀네임을 제공합니다.
- **스마트 투명도 전환**: 팝업창을 화면에 항상 위에 고정(Pin)해 둔 경우, 10초간 컴퓨터 입력이 없으면 방해되지 않도록 창이 자동으로 반투명하게 전환됩니다.
- **직관적인 한도 경고**: 안전(초록), 경고(주황), 임계(빨강) 등 한도 상태에 맞춰 트레이 아이콘과 알림선 색상이 변경됩니다.

---

## 화면 구성 예시

```
┌─────────────────────────────────────┐
│ [P] Claude Code Usage           [-] │
├─────────────────────────────────────┤
│ 5h Usage                        62% │
│ [#############.....|.....|....]     │
│ Resets in 1h 26m (18:00)            │
├─────────────────────────────────────┤
│ 7d Usage                        18% │
│ [###...............|.....|....]     │
│ Resets in 2d 3h (4/17 18:00)        │
├─────────────────────────────────────┤
│ Extra Usage                     47% │
│ [########..............]            │
│ $9.51 / $20.00                      │
├─────────────────────────────────────┤
│ (O) Next Fetch 18:35   sonnet-5 [S] │
└─────────────────────────────────────┘
```

- **`[P]` (Pin 버튼)**: 클릭 시 주황색(고정), 회색(해제)으로 변하며 항상 위에 팝업을 고정합니다.
- **`[-]` (숨기기 버튼)**: 클릭 시 현재 대시보드 창을 트레이 아이콘으로 숨깁니다.
- **활성 라인**: 타이틀바 바로 밑 점선 영역으로, 대화 수행 시 가로선이 나타나 사용 중임을 실시간 시각화합니다.
- **게이지바 내 `|` 기호**: 경고 임계선인 71%(주황) 및 86%(빨강) 지점에 표시되는 희미한 구분선입니다.
- **`(O)` (상태 표시등)**: API 서버 연결 및 갱신 상태(성공, 대기, 오류)를 색상 아이콘으로 표시합니다.
- **`[S]` (설정 버튼)**: ⚙ 기어 모양 아이콘으로, 클릭 시 하단으로 설정 패널이 확장됩니다.

---

## 실행 요구사항

- Windows 10 / 11
- Claude Code가 로컬에 설치되어 있고 로그인된 상태 (`~/.claude/.credentials.json` 자격 증명 파일 필요)

---

## 빌드 방법

소스 코드를 로컬에서 직접 컴파일하여 빌드하는 방법입니다.

```bash
# 빌드 디렉토리 구성
cmake -B build -S .

# 프로젝트 컴파일
cmake --build build --config Debug

# 실행
./build/ClaudeTray.exe
```

---

## 파일 구조

- `model_pricing.json`: 최신 Claude 모델들의 단가 요율 설정 파일 (비용 계산용)
- `credentialsreader.h/cpp`: Claude Code의 로그인 정보를 읽어오는 역할
- `usageapiclient.h/cpp`: Claude API 서버와 통신하여 실시간 사용량을 조회하는 역할
- `usagescanner.h/cpp`: 로컬 JSONL 대화 로그 파일을 실시간으로 감시하고 비용을 역산하는 역할
- `trayapp.h/cpp`: 시스템 트레이 아이콘을 구성하고 프로그램 전체 흐름을 관리하는 역할
- `usagepopup.h/cpp`: 트레이 아이콘 클릭 시 발생하는 사용량 대시보드 창 UI
- `quotapanel.h/cpp`: 사용량 게이지바와 임계 경고선 작업을 처리하는 위젯
- `toggleswitch.h/cpp`: ON/OFF 스위치 컴포넌트

---

## 라이선스

MIT License
